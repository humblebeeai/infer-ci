# About mAP50-95 per class wandb logging:
mAP50-95 for each class is logged into wandb during validation. It can be seen under the `mAP50-95` section in the wandb dashboard.
In this section, curve for mAP50-95 for each class is logged.